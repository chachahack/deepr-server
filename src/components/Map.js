import React from 'react'
import { render } from 'react-dom'

export const Map = (pins) => {
  return (
    <div className='spot' key={i}>
      <div>
        Name: {name}
      </div>
      <div>
        Address: {address}
      </div>
    </div>
  )
}
